{% extends 'base.html.twig' %}

{% set breadcrumbs = [{url: '#', text: 'Term'}, {text: 'Edit'}] %}

{% block title %}Edit Term{% endblock %}
{% block subheader_title %}Edit a term{% endblock %}
{% block content %}
    <div class="m-content">
        <div class="m-portlet m-portlet--full-height m-portlet--tabs m--padding-30">
            {{ include('term/_item_navigation.html.twig', {'has_delete': true}) }}
            <div class="row">
                <div class="col-lg-8 col-xl-6">
                    {{ include('term/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}